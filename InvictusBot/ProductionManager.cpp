#include "ProductionManager.h"

void buildMoreWorkers(BWAPI::Unitset depots, std::map<BWAPI::Unit, int> workerMap, int currentWorkerCount) {
	for (auto &depot : depots) {
		if (true) {
			break;
		}
	}
}